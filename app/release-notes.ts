/* 面向用户的版本说明——这份文件由人维护。
 *
 * 分工：版本号、发布日期、四平台安装包文件名与 SHA256 全部由
 * scripts/sync-release-data.mjs 写进 app/release-data.ts；这里只写
 * "用户能感知到什么变化"的说明文字。发布流水永远不会改写本文件。
 *
 * 发布新版本后：先跑 npm run sync:release，再在下面按版本号补一条。
 * 版本说明页会用 release.version 去查这里；查不到就只显示安装包信息，
 * 不会伪造更新说明。
 */

export type ReleaseNote = {
  /** 一句话说明这个版本的性质 */
  summary: string;
  /** 面向用户的变化要点，逐条写成完整句子 */
  highlights: string[];
};

export const releaseNotes: Record<string, ReleaseNote> = {
  "0.3.65": {
    summary:
      "内容版本：只读权限的成员现在真的能「把本地改动提成 PR」了（fork 通道四处修复），" +
      "新技能提交前会核验归组、校验凭证与重名。安装器程序本身没有改动。",
    highlights: [
      "只读权限的成员现在真的能「把本地改动提成 PR」了。组织里多数成员对内容仓只有读权限，提 PR 要走 fork（在你自己账号下复制一份仓、推到那里、再跨仓发起 PR）。此前这条路有四处问题：跨仓 PR 少了必要参数发不出去；fork 地址是拼出来的，你名下恰好有同名仓时会指向错误的仓；私有仓的 fork 是异步创建的，刚建好就推会失败、报错看起来像权限问题害你白查凭据；第二次提交时 fork 已存在会被当成错误。四处都已修复并用真实的跨仓 PR 验证过。**全程自动，你不需要知道 fork 是什么。**",
      "新技能提交前会核验三件事。治理法条要求新建技能必须经 skill-creator 建立并校验通过——现在提 PR 时会真的检查：目录归组了吗（没归会按内容建议一个组，开新组也行、在 PR 里写明边界）、有没有校验凭证（缺了会给出确切的补法）、名字与现有技能冲突吗（重名会被接线静默吃掉一个，装上去毫无提示）。改已有技能不受这些限制；旧版技能库没带校验器时只提醒不拦。",
      "修复校验凭证在用户机器上生成不出来的问题。校验工具依赖一个第三方库，而安装器铺的便携 Python 里没有它——等于要求你出示一个开不出来的证明。现在纯标准库也能跑。",
      "提交 PR 之后，工作区会自动切回同步基线。此前提交完你会停在工作分支上，之后的就地同步会一直被「不是快进」挡住，报错还误导说「版本血统不对」。现在 PR 发起后自动回到基线，同步照常；代价是你的改动在 PR 合并前本地暂不可用（都在那个分支上，git switch 随时回去），合并后跑一次就地同步就全回来了。",
      "改错位置会被当面说破。早期版本安装的技能目录是拷贝而不是链接，改那份拷贝提不上去——现在扫描前会明确告诉你哪些技能是这种形态、该改哪里。技能库新增 CONTRIBUTING.md，「升级、重装与修复」页也补了「改动要放在哪里才提得上去」的三条定位。",
    ],
  },
  "0.3.64": {
    summary:
      "内容版本：修好上一版里「把本地改动提成 PR」对治理框架不可用的问题，" +
      "并让就地同步告诉你上游新增了哪些配置项。安装器程序本身没有改动。",
    highlights: [
      "修复 0.3.63 里「把本地改动提成 PR」对治理框架不可用的问题。它被一道本不该存在的自检门挡死了：那道门要跑一个只存在于旧版框架的检查脚本，而当前框架里没有那个脚本，于是「找不到」被判成「检查不通过」，结果是治理框架的 PR 永远建不起来。这一版去掉了那道门——改动该不该进，由上游的代码评审与 CI 判断，本机只负责把你的改动变成一个能被评审的分支。**已经装了 0.3.63 的不必等这一版**：在会话里说一句「更新治理框架」，跑一次就地同步就会把这个功能修好。",
      "就地同步现在会告诉你上游新增了哪些配置项。`settings.json` 与 `project_profile.yaml` 是你自己调过的文件，同步永远不会覆盖它们——但「不覆盖」不等于「不告诉」：上游新增的权限项或配置键现在会列出来，合不合由你决定。",
      "上游改动了 MCP 服务端代码时，同步会明确提示 `.mcp.json` 由安装器维护、需要跑一次安装器升级，并顺手做一次编译冒烟，免得等到 MCP 报错时才发现对不上。",
      "修复 Linux 与 macOS 上技能接线的幂等判断。从 Python 3.12 起，判断「这是不是安装器自己接出来的链接」在这两个平台上恒为否，导致每次运行都把已接好的技能当成用户自有目录跳过。接线产物一直是对的，但状态显示不准，且上游删除技能后的残骸清理在这两个平台上完全失效。",
    ],
  },
  "0.3.63": {
    summary:
      "内容版本：治理框架与技能的更新从此可以就地同步，不必每次重装整包；" +
      "本地改过的内容也能一键生成 PR 回流。安装器程序本身没有改动。",
    highlights: [
      "框架与技能的更新现在可以就地同步。此前别人给治理框架或技能库提的改动合入之后，拿到它的唯一办法是重新下载安装包装一遍——为了几 KB 的文本改动重装一次。现在在会话里说一句「更新治理框架」，framework-sync 技能就会把两个内容仓拉到最新并刷新协议块与技能接线。它只碰内容，不动 hook 接线、MCP 配置和权限基线，所以绝大多数情况下同步完什么都不用做：不需要重启会话，也不需要在 Codex 里重新信任。只有 hook 接线的形状真的变了时，它才会告诉你「这次没应用，要完整生效请跑一次安装器升级」。",
      "首次同步前需要在机器上配一次 GitHub 凭据（`gh auth login` 后 `gh auth setup-git`）。此前内容一直是随安装包离线铺下来的，机器上从没配过 git 凭据，所以第一次跑会提示凭据缺失。取不到远端时会按六种情形（没配凭据、账号无权限、凭据过期、离线、公司代理拦截证书、上次中断留下锁文件）分别给出能照做的指引，而不是含糊地跳过。",
      "在本地改过的框架内容可以一键生成 PR 回流。governance-pr 技能现在能直接处理最常见的现场：改动还没提交、submodule 处于游离状态没有上游追踪——它会建一个工作分支、提交、推送并开 PR，没有直推权限就自动改走 fork。提交说明必须由你来写，技能不代写。",
      "治理协议块现在固定排在 CLAUDE.md 与 AGENTS.md 的最开头，先于你自己的内容被读到。升级后第一次运行会把它一次性挪到最前面，所以你会看到这两个文件有一次比较大的 diff——那只是顺序变化，块的内容与你自己写的内容都逐字节没动，相对顺序也不变。",
      "上游删掉的技能，本地接线残骸会被自动清理（此前只增不删，会永远留着一个指向空处的链接）。清理只针对确定是安装器自己接出来的链接，你自己放在技能目录里的内容一律不碰。另外修复了 Linux 与 macOS 上「是不是自己接出来的链接」判断恒为否的问题，此前这两个平台每次运行都会把已接好的技能当成用户自有目录跳过。",
    ],
  },
  "0.3.62": {
    summary:
      "修复版本：五个真机战报一次收口（Linux 复制授权码闪退、Windows 缺少 Git " +
      "死循环、升级后台验收失败、载荷解压被拒）＋全向导「上一步」。",
    highlights: [
      "修复 Linux 安装器点「复制授权码」后整个程序闪退的问题。界面组件的权限回调给系统传的是旧式数值，新版界面库只认规范枚举，回调在事件槽里抛错会让进程直接退出且没有任何提示。双层修复：运行时替换该回调（异常永不外逸），并把界面库全家族版本在构建时钉死。此问题由一位用户反编译安装包精确定位，特此致谢。",
      "修复 Windows 上「有 git 但版本较老」时的死循环：点「自动安装内置便携 Git」每次都显示装好、检测每次都不过。现在检测页面会如实显示找到了哪个 git、什么版本、差在哪；检测会兜底使用已装过的便携 Git；进程内路径优先级修正后装好即生效。",
      "修复升级旧项目时报「20 秒内没有看到当前项目状态」的问题。升级现在一律「停旧起新」重启后台 Agent，启动后数秒内直接确认进程存活；失败时的诊断会附带后台日志和进程清单，不再无从查起。后台 Agent 升到 0.4.8（启动失败必留日志；含检索工具在新版 Claude Code 上报 tools fetch failed 的协议修复）。",
      "修复安装器启动报「内置载荷解压失败 WinError 5」的问题：自动清理无效缓存残骸、等待杀毒扫描的时间拉长，最终报错会写明路径与处置办法。",
      "所有选择与确认页面右上角新增「上一步」按钮，选错目录或产品线不用重开安装器。",
    ],
  },
  "0.3.61": {
    summary:
      "修复版本：Linux/macOS 上 Codex 里检索工具报 CREDENTIALS_UNAVAILABLE、" +
      "同一台机器 Claude 却正常的问题（凭据存储方式更换）。",
    highlights: [
      "修复 Linux/macOS 上 Codex 里检索工具报 CREDENTIALS_UNAVAILABLE 的问题。此前凭据优先存进系统密钥环，而密钥环跟着桌面登录会话走：Codex 以精简环境启动检索工具的后台进程，读不到密钥环，凭据取不出来；Claude 会把完整环境传给子进程，所以同一台机器它能用。并且令牌每次自动续期都会按同样规则重写凭据文件——手工修好也会被下一次续期改回去。",
      "这一版起 Linux/macOS 凭据一律直接存在权限收紧的文件里（仅本用户可读；Windows 的 DPAPI 存储不变、不受影响）。已经写成密钥环引用的旧机器，升级后第一次成功使用会自动迁移回文件形态、并清掉密钥环里的废条目，不需要手工处理。",
      "后台 Agent 升到 0.4.7（即上述修复；升级会真正安装它）。",
    ],
  },
  "0.3.60": {
    summary:
      "修复版本：Codex 里检索工具报「connection closed」的启动问题，" +
      "已在 Ubuntu 真机 Codex 会话完整验收。",
    highlights: [
      "修复 Codex 里 records 检索工具报「connection closed: initialize response」的问题。后台桥此前在进程启动时就读本机凭据（系统密钥环）；Codex 启动子进程的环境和时序下这一步可能失败或过慢，桥在完成连接握手前就退出了。现在凭据推迟到第一次真正调用工具时才读：连接与工具列表永远先成功；凭据真读不到时返回一条写明原因与处置的错误信息，而不是整个连接消失。",
      "已在 Ubuntu 真机的 Codex 会话完整验收：治理 15 个工具与检索 9 个工具全部列出，真实调用返回数据。",
      "提示：首次使用新钩子的会话可能出现「MCP startup interrupted」提示——那是信任钩子的对话框打断了初始化，属一次性现象；用 /mcp 查看，两个 server 连上即为正常。",
      "后台 Agent 升到 0.4.6（即上述修复；升级会真正安装它）。",
    ],
  },
  "0.3.59": {
    summary:
      "修复版本：Ubuntu/macOS 上 Codex 的钩子被误删、" +
      "以及新版 Codex 连接检索工具报 MCP failed 的问题。",
    highlights: [
      "修复 Ubuntu/macOS 上 Codex 钩子被「能力探测」误删的问题。安装器此前只在 PATH 和桌面版目录里找 codex——装在 ~/.local/bin（Linux 官方安装位）的 codex 从图形界面启动的安装器里探测不到，安装器误以为这台机器没有支持钩子的 codex，把刚写好的钩子文件又删掉了。两处都改了：探测会查官方安装位；就算真探测不到，也不再删钩子文件——装上或升级 codex 后信任一次即可生效，不需要重装。",
      "修复新版 Codex（0.148 及以上）连接检索工具时报 MCP failed 的问题。后台桥在建立连接的版本应答上有一处不一致，新版 Codex 会判定连接失败——九个知识工具与两个数据工具全部不可用。这个缺陷从 0.3.56 引入时就存在，此前的验证恰好没覆盖新版 Codex 的握手方式。现已改为对任何版本 Codex 都成立的应答方式。",
      "后台 Agent 升到 0.4.5（含上一条的修复；升级会真正安装它）。装完后在 Codex 里重新信任一次钩子、并对 aiig-records 选「一律允许」，两组工具即可用。",
    ],
  },
  "0.3.58": {
    summary:
      "修复版本：老机器「重装修复」第一次会失败的问题，" +
      "以及多项目机器升级后其他项目的 Codex 配置不更新的问题。",
    highlights: [
      "从 0.3.53 或更早版本升上来的 Ubuntu/macOS 机器，此前选「重装当前版本（修复）」第一次会报协议模块缺失、要再跑一次才成功。这一版起修复和升级一样会自动自愈，一次操作修好。",
      "多项目机器上升级一个项目后，其他项目的 Codex 工具配置现在会一并更新到新版本。此前只有 Claude 侧是整机更新的，Codex 侧要逐个项目手动升级——Windows 上这意味着其他项目的 Codex 一直运行旧版组件。各项目的 Codex 钩子仍在该项目自己升级时才更新：钩子一更新就需要你重新信任一次，这个动作不该被别的项目的升级悄悄触发。",
      "Codex 接线时如果九个云端检索工具没能挂上（拿不到后台状态目录时会跳过），安装日志现在会明确写一行原因。另外提醒：这九个工具是 0.3.56 起的功能，项目还停在 0.3.55 及更早时配置里没有这一段是正常的，升级即可。",
      "曾退回 0.3.53 的机器再升级时，安装过程会自动多做一次组件重装（自愈在工作），属正常现象。",
    ],
  },
  "0.3.57": {
    summary:
      "修复版本：Ubuntu/macOS 从 0.3.53 或更早版本升级时装不上的问题，" +
      "升级路径从此会自动自愈。",
    highlights: [
      "修复从 0.3.53 及更早版本升级时报「No module named aiig_protocol.canonical」并中止安装的问题。起因是卸载旧版组件时误删了新协议包的文件；这一版起，升级过程发现组件残缺会自动全量重铺一次再继续。全新安装和从 0.3.55 之后的版本升级本来就不受影响。",
      "已经撞上这个错的机器现在就可以修：用安装器选「重装当前版本（修复）」，不需要等待或手动清理。注意撞上之后后台同步服务会在下次重启时进入崩溃循环——请尽快修复重装，不要放着。",
      "后台 Agent 升到 0.4.4，升级会真正安装它。",
      "服务端同批修复（不随安装包，但与你相关）：回流的任务文档很长（几百个小节）时，知识提取此前会无声卡住、永远到不了管理平台的审核队列；现在长文档照常处理，真正处理不了的会带着原因进入人工队列，而不是消失。",
    ],
  },
  "0.3.56": {
    summary:
      "修复版本。最要紧的一条是：装机不再要求本机装有浏览器——" +
      "此前这类机器会停在「等待 GitHub 与公司身份核验」直到授权码过期。",
    highlights: [
      "装机时的 GitHub 授权码和网址，现在直接显示在安装器页面上，可以点击也可以复制，并带剩余有效时间。此前它只以一行 JSON 出现在安装日志里：一位同事在 Ubuntu 24.04（通过 X11 转发使用、远端没装浏览器）上装 0.3.53 时，界面停在「等待 GitHub 与公司身份核验」，而紧跟其后的一串「not found」把那行 JSON 顶出了日志面板，一直等到十分钟后授权码过期。GitHub 的设备授权流程本来就允许在任意一台设备上完成——用手机打开那个地址输入授权码即可，不必在装机的这台机器上装浏览器。",
      "授权等待时间从 10 分钟放宽到 GitHub 给的完整 15 分钟。此前那 5 分钟是安装器自己砍掉的，而它正是「走到另一台设备去输码」所需要的时间。",
      "已经装了 0.3.53 或 0.3.55 的机器不必等升级：「排查问题 → GitHub 身份与授权」里写了怎么从安装日志里取出授权码完成授权。",
      "装机失败时会说出真正的原因。后台进程一直在上报精确原因（例如「GitHub 授权超时」），而安装器此前把它丢掉、只显示「请查看安装日志」——恰恰是那条你刚刚翻不到授权码的日志。",
      "修复 Codex 钩子在 Windows 上起不来：安装器渲染的命令给 Python 解释器多套了一层引号，Codex 不接受这种写法，于是五个钩子全部失败、CLI 直接报 hook failed。Linux 与 macOS 上的命令一个字节都没有改动——那两个平台的引号是必需的。",
      "装完 Codex 之后的信任步骤，CLI 与 VS Code 插件长得完全不一样：CLI 是会话启动时弹一段菜单，选第 2 项「Trust all and continue」；VS Code 插件是设置面板里按事件分组、每组一个 Trust 按钮加一个开关，要逐个点。安装器完成页新增一个按钮，点开直达这两个界面的截图说明。两步不做，治理静默不工作。",
      "Codex 侧补齐九个云端检索工具，与 Claude Code 那侧一致。此前 Codex 只有治理工具。另外，你在 Codex 里对单个工具设置的审批方式，重装安装器时会原样保留（此前会被抹掉，需要重新逐个设置）。",
      "后台 Agent 升到 0.4.3。同机多个进程共用一个身份会话时，抢刷新令牌失败的那个此前会被误判成「设备会话已失效、请重新授权」，而会话其实完全正常——这一版修好了。升级会真正安装这个新版本。",
    ],
  },
  "0.3.55": {
    summary:
      "两件面向使用者的新东西：装机时可以给工作目录选一条产品线，" +
      "以及 Codex CLI 成为与 Claude Code 并列的受治理前端。",
    highlights: [
      "装机时可以选产品线。此前管理平台上一行等于「某台电脑上的某个文件夹」，同一个真实项目换台电脑、换个目录名就成了两个项目。选了产品线之后，同一条产品线下的多个工作目录在管理平台上合并显示为一行，展开才看到它们分别在哪台机器上。产品线由管理员在管理平台上统一维护，装机时只能选、不能新建。",
      "产品线可以不选，也不影响装机。下拉第一项恒为「暂不归类」；0.3.55 之前装的机器全是未归类状态，它们不受影响、不需要重装，管理员可以事后补挂。装机现场没有外网时，下拉会自动改用安装包内置的清单，页面上会明说这批是离线的。",
      "选择记在项目目录下的 .governance/aiig-install.json 里，会随仓库提交——它是随仓走的项目属性，不是本机偏好。同事克隆仓库后首次装机，下拉会自动停在同一条产品线上；升级与重装也会记住上次的选择。",
      "Codex CLI 接入治理。安装器会写入 AGENTS.md、项目级 .codex/hooks.json 与项目级 .codex/config.toml 里的 Governance MCP。**装完之后还有两步只能你自己在 Codex 里点**：在 Settings → Hooks → From Projects 里逐个打开五个钩子并重启会话；MCP 首次询问时选「一律允许」。两步不做，治理静默不工作——不报错，只是什么都不记录。另外，Codex 按钩子文件内容的哈希记信任，所以每次升级治理框架之后要重新信任一次。",
      "Codex 的 MCP 配置写在项目自己的 .codex/config.toml 里，所以在几台机器上装几个项目都互不影响；从旧版本升级时，安装器会把此前写在用户级 ~/.codex/config.toml 的那一条收走。",
      "管理平台的概览页现在并排给出两个数：工作目录数与产品线数。上报端还不支持产品线时显示「—」，支持但一条都没归类时显示 0——两种「没有」不再混为一谈。",
    ],
  },
  "0.3.54": {
    summary:
      "Codex CLI 治理支持的首个版本，四平台同步出包。" +
      "（Codex 的 MCP 配置在本版写在用户级文件里，0.3.55 起改为项目级并会自动回收旧配置。）",
    highlights: [
      "首次支持把 Codex CLI 接入治理框架：安装器写入 AGENTS.md 与 Codex 钩子/MCP 配置，与 Claude Code 并列成为第二个受治理前端。",
      "Windows、Ubuntu/WSL、macOS（Intel 与 Apple Silicon）四平台同步出包。",
      "本版的 Codex MCP 配置为用户级（~/.codex/config.toml）——多项目机器上有局限，0.3.55 已改为项目级；如果你还停在 0.3.54，直接升级到最新版即可，无需先处理旧配置，安装器会自动回收。",
    ],
  },
  "0.3.53": {
    summary:
      "纯修复版本，主题是打通 Ubuntu 与 macOS 上的知识检索通路，" +
      "并让重装与升级的语义第一次变得明确。",
    highlights: [
      "根治 Linux/macOS 升级不更新组件的缺陷：此前共享运行时按固定版本号钉安装，包版本长期不变，pip 认为条件已满足因而什么都不装，机器上一直跑着旧的 Agent 与 Bridge。这一版把 records 包版本推到 0.4.1，并把语义写成规则——重装一律全量重铺，升级只装版本号提升的组件。",
      "知识检索通路修复：查询结果含中文或其他非 ASCII 字符时连接被中断的问题、以及后台令牌轮换竞态导致六个云端查询工具在会话开始约十五分钟后集体返回 404 的问题，均已修复。",
      "升级与重装会重新接线本机全部已挂载项目，而不只是当前选中的那个。多项目机器上其他项目不会再残留指向旧载荷的绝对路径。",
      "安装器事务修复：一次中断的卸载会留下卸载态的事务日志，之后任何安装动作都会尝试接续它并在第一次推进阶段时崩溃。现在事务日志只在同一阶段族内接续，中断的卸载不再阻塞后续的覆盖重装。",
      "知识检索工具在客户端呈现为七个：六个受治理的云端查询工具（search_knowledge、get_knowledge、related_knowledge、search_records、list_projects、knowledge_status），外加一个只读本机日志、不出本机的 diagnostic_logs。",
    ],
  },
};

/** 取某个版本的说明；没有就返回 null，页面据此降级而不是编造。 */
export function noteFor(version: string): ReleaseNote | null {
  return releaseNotes[version] ?? null;
}
